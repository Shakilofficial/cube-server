# trigger pipeline only when code is pushed to main and pull request opened for main

# name: CI Pipeline

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]

#  jobs:
#   build-and-test:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Install pnpm
#         uses: pnpm/action-setup@v3
#         with:
#           version: 9.0.5

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 24
#           cache: 'pnpm'

#      - name: Install Dependencies
#         run: pnpm install --frozen-lockfile

#      - name: Build Packages & Services
#         run: pnpm build
